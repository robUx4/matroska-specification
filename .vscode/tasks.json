{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile XSD",
            "type": "shell",
            "command": "C:\\msys64\\usr\\bin\\env.exe",
            "args": ["xsltproc", "-o", "${workspaceFolder}\\matroska_ids.h", "${file}", "${workspaceFolder}\\matroska_xsd.xml"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}